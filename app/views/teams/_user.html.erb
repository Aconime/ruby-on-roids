<tr>
  <th scope="row"><%= user.id %></th>
  <td><%= user.name %></td>
  <td><%= user.email %></td>
  <td>
    <div class='action-btns'>
      <% if Current.user.id == team.owner_id %>
        <%= button_to 'Kick', root_path, class: 'action-btn kick-btn' %>
      <% end %>
      <%= button_to 'View', root_path, class: 'action-btn view-btn' %>
    </div>
  </td>
</tr>